# =================================
# SKILLSWAP - Docker Environment (DEV)
# =================================
# Copier ce fichier vers .env.docker et remplir les valeurs
# cp .env.docker.example .env.docker
#
# Pour la PRODUCTION, voir .env.prod.example

# Database
POSTGRES_USER=skillswap
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=skillswap
DATABASE_PORT=5433
DATABASE_URL=postgresql://skillswap:your_secure_password_here@postgres:5432/skillswap

# API Backend
# Note: le backend écoute sur le port 3000 en interne
API_PORT=3000
ALLOWED_ORIGIN=http://localhost:8888
JWT_SECRET=your_very_long_and_secure_jwt_secret_min_32_chars
TOKEN_EXPIRE=3600

# Frontend Next.js
# Note: le frontend écoute sur le port 3000 en interne
CLIENT_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:8888

# API interne (pour les appels serveur dans Docker - sitemap, SSR, etc.)
# IMPORTANT: Cette variable est nécessaire pour que le frontend puisse
# appeler le backend depuis le réseau Docker (server-side rendering)
INTERNAL_API_URL=http://backend:3000

# SEO - URL publique du site
NEXT_PUBLIC_SITE_URL=http://localhost:8888

# Meilisearch
MEILI_MASTER_KEY=dev_secret_key_min_16_chars
MEILISEARCH_HOST=http://meilisearch:7700
# Backend uses MEILI_API_KEY (use master key in dev)
MEILI_API_KEY=dev_secret_key_min_16_chars

# Adminer
ADMINER_PORT=8080
