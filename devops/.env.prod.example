# =================================
# SKILLSWAP - Production Environment
# =================================
# Copier ce fichier vers .env.docker sur le VPS
# cp .env.prod.example .env.docker
#
# IMPORTANT: Remplacer TOUTES les valeurs par des valeurs sécurisées !
# Générer des secrets avec: openssl rand -base64 32

# Database
POSTGRES_USER=skillswap
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=skillswap
DATABASE_URL=postgresql://skillswap:CHANGE_ME_STRONG_PASSWORD_HERE@postgres:5432/skillswap

# API Backend
API_PORT=3000
ALLOWED_ORIGIN=https://skill-swap.fr
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32
TOKEN_EXPIRE=3600

# Frontend Next.js
CLIENT_PORT=3000
NEXT_PUBLIC_API_URL=https://skill-swap.fr

# API interne (pour les appels serveur dans Docker - sitemap, SSR, etc.)
INTERNAL_API_URL=http://backend:3000

# SEO - URL publique du site
NEXT_PUBLIC_SITE_URL=https://skill-swap.fr

# Meilisearch
MEILI_MASTER_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32
MEILISEARCH_HOST=http://meilisearch:7700
MEILI_API_KEY=CHANGE_ME_SAME_AS_MEILI_MASTER_KEY
